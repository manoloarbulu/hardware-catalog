@workspace /agents/frontend/react-ts-engineer.md /skills/frontend/axios-integration.md

I need a new React UI view for managing `[ENTITY_NAME]`.

Please generate:

1. The TypeScript interfaces for `[ENTITY_NAME]` in `src/types`.
2. The Axios service calls (GET, POST, PUT, DELETE) in `src/services/[ENTITY_NAME]Service.ts`.
3. A main List Component that fetches and displays the data in a responsive grid or table using Tailwind CSS.
4. Include basic loading spinners and error state handling.
