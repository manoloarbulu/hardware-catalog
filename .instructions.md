Act as an expert Full-Stack .NET Developer and Software Architect. I need you to generate a complete, working demo portal consisting of two solutions: a .NET 10 Web API backend and a React (TypeScript) Single Page Application (SPA) frontend.

Please provide the implementation following these detailed specifications:

### 1. GENERAL REQUIREMENTS & SECURITY

- **Package Integrity:** Ensure all requested NuGet and NPM packages are the latest stable versions compatible with .NET 10 and React 18+. Do not use any deprecated packages or libraries with known security vulnerabilities.
- **No Authentication:** Do NOT include any authentication or authorization (e.g., no JWT, no Identity). All endpoints must be completely open and anonymous for testing purposes.

### 2. BACKEND SOLUTION (.NET 10 Web API)

Use Clean Architecture principles. Separate the solution into four projects: Domain, Application, Infrastructure, and WebApi (Presentation).

**Architecture & Libraries:**

- Implement the **CQRS pattern using MediatR**.
- Integrate **FluentValidation**. Configure it as a MediatR Pipeline Behavior in the Application layer so that incoming Commands and Queries are automatically validated before reaching their handlers.
- Configure **Swagger** (using the new built-in .NET OpenAPI features or Swashbuckle) for API documentation. Ensure the Swagger UI middleware is strictly wrapped in an `if (app.Environment.IsDevelopment())` block in `Program.cs`.

**Database & ORM:**

- Use Entity Framework Core (EF Core) with a Code-First approach.
- Target a SQL Server database named `ProductsDemo`.
- Implement automatic database creation and migrations on application startup. If the database does not exist, it must apply migrations and seed initial data automatically.

**Domain Entities & Enums:**

- **Enums:**
  - `ProductCategory`: Memory, Storage, ExternalPorts, GraphicCard, PowerSupply, Processor.
  - `UnitOfMeasure`: MB, GB, TB, Units, Watts, Voltage.
  - `WeightUnit`: Pounds, Kilograms, Grams
  - `ComputerType`: Laptop, Desktop, Server, Blade Server
- **Product:** `Id` (Guid), `Category` (Enum), `Name` (String), `UnitOfMeasure` (Enum), `Brand` (Foreing Key reference), `Model` (String).
- **Computer:** `Id` (Guid), `CreationDate` (DateTime), `Type` (Enum), `Weight` (Decimal), `WeightUnit` (Enum). Suggest more columns for this entity.
- **ComputerProduct (Junction Entity):** Because a Computer contains multiple Products and we need to track quantities, create this many-to-many junction table containing `ComputerId`, `ProductId`, and `Quantity` (Int). This Quantity must never be zero. It couldn't exists zero items
- **Brand:** `Id` (Guid), `Name` (String)

**Data Seeding:**
Seed the `Products` table with realistic hardware data. Examples must include at least:

- Processor: Intel® Core™ i5-6400
- Processor: Intel® Celeron™ N3050
- Processor: AMD FX 4300
- Processor: AMD Athlon Quad-Core APU Athlon 5150
- Processor: AMD FX 8-Core Black Edition FX-8350
- Processor: AMD FX 8-Core Black Edition FX-8370
- Processor: Intel Core i7-6700K 4GHz
- Processor: Intel Core i7 Extreme Edition 3 GHz
- Graphic Card: NVIDIA GeForce GTX 1080
- Graphic Card: NVIDIA GeForce GTX 960
- Graphic Card: Radeon R7360
- Graphic Card: NVIDIA GeForce GTX 770
- Graphic Card: Radeon RX 480
- Graphic Card: Radeon R9 380
- Graphic Card: AMD FirePro W4100
- Memory: Kingston 8 GB DDR5
- Memory: Kingston 16 GB DDR5
- Memory: Kingston 32 GB DDR5
- Memory: Kingston 512 MB
- Storage: Western Digital 1TB SSD
- Storage: Seagate 2TB HDD Barracuda
- Storage: Seagate 3TB HDD Barracuda
- Storage: Seagate 4TB HDD Barracuda
- Storage: Seagate 512GB SDD
- Storage: Seagate 750GB SDD
- Storage: Seagate 256GB SDD
- Storage: Western Digital 2TB SSD
- Power Supply: Corsair 500W PSU
- Power Supply: MSI 1000 W PSU
- Power Supply: MSI 450 W PSU
- Power Supply: MSI 750 W PSU
- Power Supply: Corsair 508 w PSU
- External Ports: USB 3.0
- External Ports: USB 2.0
- External Ports: USB C

Looks for standard products for computers like the list described here and add them to the seed data as well
Seed the `Brands` table with realistic brands names like: Dell, HP, Lenovo, IBM, Corsair, Western Digital, Kingston, NVIDIA and so on.

**Endpoints (WebApi Controllers):**
Controllers should be ultra-thin, primarily dispatching to MediatR.

- **CRUD for Computers:** Standard endpoints (GET all, GET by id, POST, PUT, DELETE). When creating or retrieving a `Computer`, the payload must include its nested `Products` and their respective `Quantity`. Include FluentValidation rules for the POST/PUT commands (e.g., ensuring a computer has at least one product).
- **Natural Language Search:** Create a `GET /api/products/search?query={text}` endpoint. This should take a natural language query (e.g., "Show me 1TB storage drives") and return matching `Product` entities. Implement a basic keyword extraction/LINQ search for this demo, structured so an AI service could easily replace the logic later.

### 3.5 BACKEND TESTING

Create a dedicated **HardwareCatalog.Tests** project using XUnit and Moq for comprehensive testing.

**Testing Framework & Tools:**

- Use **XUnit** as the testing framework
- Use **Moq** for mocking dependencies (repositories, services, contexts)
- Use **FluentAssertions** for more readable assertions
- Use **EntityFrameworkCore.InMemory** for database testing without requiring SQL Server

**Test Coverage Requirements:**

1. **Validator Tests** - Test all FluentValidation validators
   - Valid commands/queries should pass validation
   - Invalid inputs (negative weight, zero quantity, missing products) should fail with appropriate error messages
   - Example: `CreateComputerCommandValidatorTests`, `UpdateComputerCommandValidatorTests`, `SearchProductsQueryValidatorTests`

2. **Handler Tests** - Test CQRS command and query handlers
   - Test successful CRUD operations (Create, Read, Update, Delete)
   - Test error handling and edge cases
   - Use in-memory database for data persistence tests
   - Example: `CreateComputerCommandHandlerTests`, `GetAllComputersQueryHandlerTests`, `SearchProductsQueryHandlerTests`

3. **Entity Tests** - Test domain entity behavior
   - Verify entity constructors and property validations
   - Test navigation properties and relationships

4. **Integration Tests** (Optional but Recommended)
   - Test the full pipeline with MediatR validation pipeline behavior
   - Verify CORS configuration
   - Test API endpoint responses with sample data

**Minimum Test Examples:**

- Validator: Creating a computer with valid/invalid weight, quantity validation
- Handler: Querying computers, handling not-found scenarios
- Handler: Search products with keyword extraction

**Running Tests:**

```bash
cd backend
dotnet test
```

**Test Project Structure:**

```
HardwareCatalog.Tests/
├── Validators/
│   ├── CreateComputerCommandValidatorTests.cs
│   ├── UpdateComputerCommandValidatorTests.cs
│   └── SearchProductsQueryValidatorTests.cs
├── Handlers/
│   ├── CreateComputerCommandHandlerTests.cs
│   ├── GetAllComputersQueryHandlerTests.cs
│   ├── GetComputerByIdQueryHandlerTests.cs
│   └── SearchProductsQueryHandlerTests.cs
├── Entities/
│   ├── ComputerEntityTests.cs
│   └── ProductEntityTests.cs
└── HardwareCatalog.Tests.csproj
```

### 3. FRONTEND SOLUTION (React + TypeScript)

Create a modern SPA to consume the .NET 10 API.

**Tech Stack:** React (Functional components, Hooks), TypeScript, Vite (for bundling), and Tailwind CSS for layout. Use Axios for HTTP requests (leveraging interceptors for base URLs or global error handling).

**Required Features:**

- **Dashboard/List View:** A table or grid displaying all existing `Computer` entities, showing their creation dates and a summary of their components.
- **Computer Management (Create/Edit/Delete):** Forms to handle CRUD operations. Users must be able to select `Products` from a type-ahead dropdown, specify the `Quantity` that must be at least 1, and attach them to the `Computer`. Implement basic client-side validation before sending data to the API. The computer Weight and Weight Unit are required, the control for Weight must accept up to 1 decimal.
- **Natural Language Product Search:** A dedicated UI section with a search bar. It must call the search endpoint and display the resulting matching products in a responsive grid or list.

### Deliverables

1.  The complete folder structure and CLI commands to initialize the .NET 10 projects (Domain, Application, Infrastructure, WebApi, Tests) and the React Vite app.
2.  The core C# classes (Entities, DbContext, MediatR Commands/Queries/Handlers, FluentValidation Validators, Pipeline Behaviors, and API Controllers).
3.  The `Program.cs` configuration showing Swagger, MediatR, FluentValidation, and EF Core setup.
4.  The React TypeScript components and Axios service layers for the required UI views.
5.  **Backend Unit and Integration Tests** using XUnit, Moq, and FluentAssertions with test cases for:
    - Validators (positive and negative test cases)
    - Command Handlers (CRUD operations and edge cases)
    - Query Handlers (data retrieval and filtering)
    - Entity behavior and relationships
6.  Clear instructions on how to run both the backend tests and the frontend simultaneously with the backend server.
7.  A test command setup in the solution for easy execution: `dotnet test`
